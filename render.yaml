services:
  - type: worker
    name: nifty-oi-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    autoDeploy: true
    envVars:
      - key: EMAIL_FROM
        value: "pranavwanderer@gmail.com"

      - key: EMAIL_TO
        value: "chakorolukur75@gmail.com"

      - key: EMAIL_PASSWORD
        value: "lxyr rgrs vpru cdyx"

      - key: WHATSAPP_ENABLED
        value: "False"